📦 Create a React App (with Vite)
----------------------------------

- npm create vite@latest
➡️ Choose:
Framework: React
Variant: JavaScript

- cd your-project-name
- npm i
- npm run dev

🧹 Clean up your project
-------------------------

Delete the files you don't need:
App.css
logo.svg
Any boilerplate in App.jsx

--------------------------------------------

npm install tailwindcss @tailwindcss/vite
 modify vite.config.ts
import { defineConfig } from 'vite'
import tailwindcss from '@tailwindcss/vite'
export default defineConfig({
  plugins: [
    tailwindcss(),
  ],
})
 Import Tailwind CSS
- @import "tailwindcss";
- import tailwindcss from '@tailwindcss/vite'
 run your react app
npm run dev

npm install react-icons --save
import { FaBeer } from 'react-icons/fa';

class Question extends React.Component {
  render() {
    return <h3> Lets go for a <FaBeer />? </h3>
  }
}


--------------------------------------------------

import React, { useState } from 'react'

export default function Main() {
    const [numb , setNumb] = useState(0)
    const numPlus = () =>{
        setNumb(numb+1)
    }

  return (
    <>
    <div className="w-[100vw] h-[100vh] bg-amber-100 flex justify-center items-center ">
        <button className="bg-amber-700 w-[15vw] h-[10vh] rounded-xl text-amber-100 text-5xl cursor-pointer hover:bg-amber-600 active:bg-amber-900 " onClick={numPlus} >{numb} like</button>
    </div>
    </>
  )
}

-------------------------------------------------

# Images 


import { React } from "react";
import { Images, Videos } from "../constants";
import watch from "../assets/images/item-03.jpg";

export default function Shop() {
  return (
    <div>
      <h1>Shop </h1>
      {/* method 1 */}
      <img src={watch} alt="" />

      {/* method 2 */}
      <img src={Images.converse} alt="" />
      <img src={Videos.imagevideo} alt="" />
    </div>
  );
}


### Method 2 : 

**1 -  create folder constant -> src**

**2-  create index.js**

**3- create  images.jsx**

**4 - export  an object  of images  paths from  images.jsx **


import converse from  '../assets/images/item-cart-02.jpg'
import watch from '../assets/images/item-cart-03.jpg'

export default {converse, watch}


** export  all images  from  index.js ** 


import Images from '../constants/images'
import Videos from '../constants/videos'

export {Images, Videos}


---------------------------------------------------------------------


## 1. Install EmailJs

npm install --save @emailjs/browser

## 2. Import it in your component 

import emailjs from '@emailjs/browser';

## 3. Usage

const [name, setName] = useState("");
  const [email, setEmail] = useState("");
  const [message, setMessage] = useState("");

  const sendEmail = () => {
    //* get the serviceId, templateId, publickey from your EmailJs  account
    const serviceId = "service_qwylvcq";
    const templateId = "template_y6jbkrc";
    const publikKey = "ybf8AyCXChqT6KH3l";

    //* creare Template Params Object
    const tempalteParams = {
        form_name: name,
        form_email: email,
        to_name: 'Sara Chafik Idrissi',
        message: message
    }

    //* Send Email Via email js
    emailjs.send(serviceId, templateId,tempalteParams, publikKey)
        .then((response) => {
            console.log("Email Sent Successfully", response);
            setName('')
            setEmail('')
            setMessage('')   
        }).catch((error) => {
            console.log("Error Sending Email", error);
        })
  };


--------------------------------------------------------------


